import { ItempofilterPipe } from './itempofilter.pipe';

describe('ItempofilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ItempofilterPipe();
    expect(pipe).toBeTruthy();
  });
});
