import { ItemstatusPipe } from './itemstatus.pipe';

describe('ItemstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemstatusPipe();
    expect(pipe).toBeTruthy();
  });
});
